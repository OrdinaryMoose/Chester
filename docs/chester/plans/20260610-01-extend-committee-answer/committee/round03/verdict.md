# Verdict — settled design of record + authoring round — round 03

**Answer shape:** converged (designer-settled).

**Round type:** authoring round. No member deliberation — round01 reached a 4-0 converged core and a single 3-1 placement split; the designer reviewed round01 and resolved the split. Round03 records the settled design and produces the sprint design brief. Round02 is parked (see `committee/round02/PARKED.md`).

**Settled design (Thread A — member-side warrant contribution):**

1. **Member stance = warranted advocacy, not synthesis.** Members keep the advocate role and the four-lens friction. A member writes its position + the ground under its load-bearing claim; it does NOT anticipate or pre-assemble the lead's synthesis. (4-0 core; role-bleed guard from Purist, echoed by Innovator's rejection of a full contract rewrite.)
2. **Placement = a discrete, dedicated warrant field (Option 1).** The member's stated ground lives in its own labeled field inside the Final Position content schema — not folded into `rationale`. Designer-chosen over Conservator's fold-into-rationale, on the verifiability/enumerability warrant: a discrete field gives the Consolidator a distinct thing to enumerate and the team-lead's Authority Guard a clean structural slot to verify, instead of a prose re-read.
3. **Warrant is typed + sourced.** The field carries a warrant type — evidence / logic / in-scope designer-premise — plus a source (citation, inference step, or the designer statement that granted the premise). (4-0: even the placement-minority required type + source.)
4. **Lead shifts origination → verification.** team-lead.md v0010 Authority Guard becomes a verification pass over member-supplied warrants rather than an origination pass. (Evidence: v0010 warrant test + feature brief Thread A.)
5. **Content extension, mechanics frozen.** The new field extends the Final Position *content* schema only. Routing-signal schema, Consolidator enumerate-only boundary, round-folder discipline, write-then-send sequencing — all unchanged. The brief states this boundary explicitly to neutralize the C-RIGID-strictness concern Conservator raised.

**Preserved dissent (recorded, not collapsed):** Conservator held that adding a fourth field to the single-authority Final Position schema is a real surface-widening on a deliberately-frozen contract, and that the ground already lives implicitly in `rationale`. The designer accepted this cost in exchange for a structurally-checkable Authority Guard, and resolves the C-RIGID worry by fixing the content-vs-mechanics line in writing.

**Deliverable:** sprint design brief at `design/20260610-01-extend-committee-answer-design-00.md`.

**Gaps / out of scope:** Threads B (session-artifact owner), C (both-sides-of-a-split layout), D (threshold wave-off wording) remain open by design — this sprint scopes Thread A only.
