# Alignment map — round 03

**Question:** Best HOME for the whole per-round flow on functional merit, member-protocol-fragment framing struck. Contested axis: which file holds the authoritative numbered list.

**Answer-shape marker:** CONVERGED (3-1) — warranted, not count-driven; one preserved minority caveat with a stated mitigation.

## Alignment pattern

- **Option 3 (two-altitude, strict non-overlap) — 3: Innovator, Pragmatist, Purist.** SKILL.md carries named phases + one-sentence descriptions (no numbered list, no execution detail); team-lead.md carries the single numbered checklist with full elaboration.
- **Option 1 — 1: Conservator.** Whole numbered sequence in SKILL.md; team-lead.md names steps without integers.

All four now agree on the structural invariant: exactly ONE numbered list should exist; the other file carries non-numbered prose. The residual disagreement is only which file holds the integers — and that reduces to a single drift-visibility question.

## The decisive warrant (why the lean is warrant, not count)

1. **The step-2 stub is an unfinished relocation (evidence).** `team-lead.md:96` step 2 reads "Per-round flow runs — per SKILL.md Phase 4 § Per-Round Flow." All three majority members independently identified this as a hollow deferral that forces a cross-file read *mid-execution*, inside an active step — not a convene-time read. It is direct evidence the design already located execution in team-lead.md but did not complete the move.
2. **Drift asymmetry (logic, Innovator).** Execution-drift (a wrong step in the checklist the team-lead runs) is fatal; description-drift (a stale prose overview) is tolerable. So the authoritative numbered checklist belongs where execution happens (team-lead.md); SKILL.md's overview can safely be the lower-stakes representation.
3. **Strict non-overlap makes residual drift visible (Purist).** SKILL.md names; team-lead.md numbers + elaborates. The representations cover non-overlapping information, so a name in SKILL.md with no matching team-lead step is a *visible* gap, not a silent contradiction — which is the current failure mode of two parallel numbered lists.

## Conservator's preserved caveat (minority, with mitigation)

- **Position:** Option 1 — keep the numbered sequence in SKILL.md; team-lead.md names steps without integers.
- **Warrant (evidence):** team-lead.md:96 proves SKILL.md is a *live* runtime citation, so a sequence there is not operationally inert; both files are pre-read before TeamCreate.
- **Core concern:** Option 3's prose-overview-in-SKILL.md must co-evolve silently with team-lead.md's checklist — *invisible* drift, which Conservator judges worse than today's *visible* collision.
- **How the majority answers it:** the strict-non-overlap construction is designed precisely to prevent the "prose ghost" — SKILL.md carries names only (not a parallel checklist), so drift surfaces as a visible name/step mismatch. And the step-2 stub Conservator cites supports *filling* the stub (completing the move into team-lead.md), not keeping integers in SKILL.md.
- **Mitigation to carry into the edit:** a comment in SKILL.md prohibiting re-introduction of a numbered list there (Innovator), so future editors don't recreate the two-checklist failure mode.

## Uncontested across all four

- Exactly one numbered list; the other file is non-numbered prose.
- The current two-numbered-list state is the failure mode being fixed.
- The 11-step inflation / three phantom entries (ledger, checkpoint, designer-response) relocate to their existing sections regardless.
- Migration cost zero today — no external file cites either step list.
